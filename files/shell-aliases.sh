# Claude Code aliases
alias c='claude'
alias ch='claude --chrome'

# --fs → --fork-session shortcut
claude() {
  local args=()
  for arg in "$@"; do
    if [[ "$arg" == "--fs" ]]; then
      args+=("--fork-session")
    else
      args+=("$arg")
    fi
  done
  command claude "${args[@]}"
}
